\usepackage[table]{xcolor} % NOTE: this must be top in the list because otherwise it clashes with another package (tikz, I believe, but not certain)
\usepackage{tikz}
\usetikzlibrary{positioning,shapes}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{wrapfig}
\usepackage{float}
\usepackage{colortbl}
\usepackage{pdflscape}
\usepackage{tabu}
\usepackage{threeparttable}
\usepackage{etoolbox}
\usepackage{soul} % For the \ul command to get underlining that wraps

%% Document formatting

%% Specify sans-serif fonts
\renewcommand{\familydefault}{\sfdefault}

\BeforeBeginEnvironment{tabular}{\sffamily}

%% Make tikz pictures relative to the page
\tikzset{%
  missing/.style = {minimum width = 0.64\textwidth, minimum height = 0.2\textheight, font = \large},%
  present/.style = {minimum width = 0.64\textwidth},%
  every picture/.style = {remember picture, overlay, inner sep = 0pt, line width=0pt,/utils/exec={\sffamily}}}

%% Define colours
\definecolor{blue}{HTML}{003D7E}
\definecolor{gold}{HTML}{FCCA06}
\definecolor{grey}{HTML}{505050}
\definecolor{lightblue}{HTML}{C2E4FF}
\definecolor{lightgrey}{HTML}{BABABA}
\definecolor{tableblue}{HTML}{3f78d3}
\definecolor{tablegrey}{HTML}{dae2ef}

%% Get underlined links

\hypersetup{%
  colorlinks=true,%
  linkcolor=lightblue,%
  urlcolor=lightblue%
}

\newcommand{\link}[3][grey]{\href{#2}{\color{#1}\ul{#3}}}

